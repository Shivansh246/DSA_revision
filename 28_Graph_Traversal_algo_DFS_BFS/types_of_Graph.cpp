/*
Directed Graph =>
no bidirectional edges, if one node points to other that does not mean that the other node will point to previous node

Weighted Graph =>
In unweighted graph all the edges are equivalent or all carries the weight of 1 and if there is no edge the weight is 0
in weighted graph the edge will also carry the weight i.e. while updating the adjacency matrix we will store matrix[i][j] = w,
where w is the weight of the edge,
weight can have the analogy of cost of travel through a route , if the weight is more through a route then we can say that the has
more weight
*/